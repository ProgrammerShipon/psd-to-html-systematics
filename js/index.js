// Jquery Active start

$(document).ready(function () {
  // Bx Slider Active
  $('.slider_area').bxSlider();
  $('main_menu').meanmenu({
    meanMenuContainer: '#mobile_menu',
    meanMenuWidth: 768,
  });
});

// Jquery Active end

